# AI分身系统 — 技术逻辑规范与阶段任务 v1.0

> 本文档综合《AI分身公司技术完成标准02》与《功能实现说明》，涵盖核心技术架构、合约规范、数学模型及开发阶段计划。

---

## 一、技术架构总览

### 1.1 分层架构

```
[ 用户/本体 ]
    ↓ 认知所有权证明
[ 身份层 ] DID 12720 + .soul SBT (ERC-721不可转移)
    ↓
[ 交互层 ] MCP协议 + LangGraph/CrewAI（多智能体编排）
    ↓
[ 共识层 ] ECE情绪预言机（多签 + 滑动窗口）
    ↓
[ 经济层 ] x402微支付 + 动态分账 (70/20/10)
    ↓
[ 存储层 ] IPFS/Arweave分片 + 链上状态根哈希
    ↓
[ 链层 ] Base L2 (MVP) → AFC主网 (UUPS平滑迁移)
```

### 1.2 技术选型

| 层级 | 技术选型 | 标准要求 |
|------|---------|---------|
| 身份层 | DID 12720 + ERC-721 SBT | 绑定钱包 + 认知根哈希不可篡改 |
| 合约层 | Solidity 0.8.24 + UUPS代理 | OpenZeppelin v5，可升级，重入防护 |
| 交互层 | MCP + LangGraph/CrewAI | JSON-LD统一上下文，权限沙盒隔离 |
| 支付层 | x402 + ERC-20 (AFC/USDC) | 微支付流 $0.01级，自动分账路由 |
| 存储层 | IPFS / Arweave | 分片加密，链上仅存哈希 |
| 前端 | Next.js 14 + Wagmi + Tailwind | i18n/a11y/响应式，INP ≤ 200ms |
| 索引 | The Graph（子图） | 链上事件实时索引 |
| 监控 | Prometheus + Alertmanager + Superset | 实时告警 + 可视化看板 |
| CI/CD | GitHub Actions + Foundry | 编译/测试/静态分析/自动部署 |

---

## 二、核心合约规范

### 2.1 工程目录结构

```
afc-avatar-protocol/
├── src/
│   ├── core/
│   │   ├── AvatarCore.sol         # DID/Soul绑定 + 认知状态根管理
│   │   └── SkillVault.sol         # 收益阈值解锁 + MCP路由注册
│   ├── governance/
│   │   ├── IFDRouter.sol          # 个体流体民主权重委托执行
│   │   └── ECEOracle.sol          # 情绪共识预言机（EIP-712签名验证）
│   ├── economics/
│   │   ├── DynamicSplitter.sol    # 70/20/10动态分账 + 金库自治
│   │   └── TokenVault.sol         # AFC代币质押 + 通缩燃烧
│   └── security/
│       └── CircuitGuard.sol       # 分级熔断控制器
├── test/                          # ≥ 15个Foundry单元测试
├── script/                        # 部署/升级/权限初始化脚本
├── audits/                        # Slither/Certora配置文件
└── foundry.toml                   # 多环境配置（default/audit/base-mainnet）
```

### 2.2 合约接口标准

**AvatarCore 核心接口**

```solidity
interface IAvatarCore {
    struct AvatarProfile {
        address owner;           // 本体地址（最终确权）
        uint256 soulId;          // .soul SBT Token ID
        bytes32 cognitionRoot;   // 认知状态根哈希
        uint256 resonanceScore;  // 情绪共振指数 [0, 100]
        uint256 avatarBalance;   // 分身自治金库余额（AFC）
        bool isFrozen;           // 认知熔断状态
        uint256 createdAt;
        uint256 lastActivityAt;
    }

    function createAvatar(uint256 soulId, bytes32 cognitionRoot) external returns (uint256);
    function updateCognitionRoot(uint256 soulId, bytes32 newRoot, bytes memory eceProof) external;
    function getAvatarProfile(uint256 soulId) external view returns (AvatarProfile memory);
    function verifyCognitiveOwnership(uint256 soulId, address claimer) external view returns (bool);
}
```

**DynamicSplitter 分账逻辑**

- 基础配置：`humanBps = 7000`，`avatarBps = 2000`，`protocolBps = 1000`
- 金库动态区间：`clamp(calculated, 1500, 2500)`
- 调整公式：`avatarAdj = (70 - resonanceScore) × 50` BPS（共振低时金库比例升，鼓励迭代）
- 分账守恒不变量：`humanAmt + avatarAmt + protocolAmt == amount`（Certora验证）

**CircuitGuard 熔断状态机**

```
resonanceScore >= 70  → NORMAL（无限制）
50 <= score < 70       → SOFT_LIMIT（阻断 isHighRisk=true 操作）
score < 50             → HARD_PAUSE（全面暂停，需多签恢复）
72小时内无人响应        → 流体民主临时接管（可配置）
```

### 2.3 安全规范

| 要求 | 实现方式 |
|------|---------|
| 可升级性 | UUPS代理模式，合约冻结期7天，治理多签授权 |
| 重入防护 | 所有状态变更函数使用 `ReentrancyGuard` |
| 权限控制 | `AccessControl`角色体系：ORACLE_ROLE / UPGRADER_ROLE / GUARDIAN_ROLE |
| 形式化验证 | Certora不变量（分账守恒 / 权重归一化 / 熔断拦截） |
| 静态分析 | Slither，排除低级信息告警，高危必须修复 |
| Gas优化 | optimizer_runs = 200（MVP）/ 20000（审计版），via_ir 按需开启 |

### 2.4 测试覆盖要求

- 语句覆盖率 ≥ 95%，分支覆盖率 ≥ 90%
- 必须覆盖：分账守恒、熔断触发/拦截、权重计算、委托撤销原子性
- Fuzz测试：runs = 256，针对分账金额和共振分边界值
- 形式化不变量：分账守恒律、IFD权重归一化、PoUE衰减单调性

---

## 三、数学模型规范

### 3.1 认知权重函数（IFD核心）

```
W(i,d,t) = λ₁·C(i,d,t) + λ₂·H(i,t) + λ₃·R(i,t) + λ₄·T(i,t) - λ₅·D(i,t)

其中：
  C = 领域认知匹配度（技能向量余弦相似度），λ₁ = 0.30
  H = 历史贡献归一化值，λ₂ = 0.25
  R = 情绪共振分（ECE输出 / 100），λ₃ = 0.25
  T = 近期表现（7天成功率 × 时效系数），λ₄ = 0.15
  D = 时间衰减惩罚（γ × Δt / t_max），λ₅ = 0.05，γ ∈ [0.05, 0.15]
  
所有λ之和 = 1，归一化后：W̃(i,d,t) = W(i,d,t) / Σ W(j,d,t)
```

**稳定性保证**：Lyapunov函数 V(W) = Σ(W_i - W*)² 在任务流平稳时 dV/dt ≤ 0，系统渐近收敛。

### 3.2 ECE情感过滤算子

```
W_filtered(t) = W(t) × [1 - α × max(0, |R(t) - R_base| / R_threshold - 1)]

参数：
  R_base = 70（本体初始共振基线）
  R_threshold = 15（偏离容忍阈值）
  α ∈ [0.1, 0.4]（惩罚强度系数）
  
偏离 > 2×R_threshold → 触发 SOFT_LIMIT，权重衰减至 0.6W
```

### 3.3 AFC代币通缩循环

```
dS/dt < -θ × β × I(t) / S(t)

其中：
  S(t) = 流通量
  θ = 5%（燃烧率）
  β = 20%（金库回购率）
  I(t) = 协议收入
  
价值捕获率 η = AFC消耗量 / AFC流通量，随生态活跃度单调递增
```

---

## 四、经济模型参数

### 4.1 代币参数

| 参数 | 数值 |
|------|------|
| 总供应量 | 1,000,000,000 AFC |
| 初始流通 | 20,000,000 AFC（$2M USD） |
| 锚定价格 | $0.1 / AFC |
| 代币用途 | 算力租赁、治理权重、LP流动性、训练激励 |
| 燃烧机制 | 交易手续费50%燃烧 + 50%LP奖励 |

### 4.2 订阅定价

| 档位 | 月费 | 分身数量 | 主要功能 |
|------|------|---------|---------|
| Starter | $49 | 1个 | 基础技能库、x402支付、.soul SBT |
| Pro | $99 | 3个 | ECE共振校准、流体民主委托、优先算力 |
| Enterprise | $299 | 集群 | API开放、合规插件、跨分身协作网络 |

### 4.3 代币分配

| 类别 | 比例 | 释放机制 |
|------|------|---------|
| 生态激励池 | 40% | 按分身认知活跃度线性释放（4年） |
| 流动性锁定池 | 25% | 收益自动复投 + 部分解锁 |
| 团队/顾问 | 15% | 4年线性 + 1年cliff |
| 社区治理 | 12% | 流体民主投票分配 |
| 战略储备 | 8% | 紧急治理触发 |

---

## 五、阶段开发任务计划

### Phase 0：基础设施（Week 1–2）

**目标**：核心合约部署至Base Sepolia，基础功能端到端跑通

| 任务 | 负责方向 | 完成标准 |
|------|---------|---------|
| 初始化Monorepo（Foundry + Next.js） | 工程 | CI绿色，编译无警告 |
| 部署AvatarCore + DynamicSplitter代理 | 合约 | Sepolia合约验证通过 |
| ECE Oracle Mock服务上线 | 后端 | 共振分更新延迟 < 500ms |
| x402支付沙盒测试 | 支付 | 首笔$0.01测试结算成功 |
| The Graph子图初始化 | 数据 | 核心事件可索引 |

**门禁条件**：测试覆盖率 ≥ 90%，无Critical漏洞

### Phase 1：MVP产品（Week 3–4）

**目标**：100创作者功能完整，数据看板上线

| 任务 | 负责方向 | 完成标准 |
|------|---------|---------|
| IFD委托界面与合约联调 | 全栈 | 委托权重更新延迟 < 1s |
| 技能库解锁逻辑完整测试 | 合约+产品 | 所有阈值场景通过Fuzz测试 |
| CircuitGuard软/硬熔断演练 | 风控 | 触发率符合预期（5%–8%） |
| Superset看板部署 | 数据 | 4个核心看板实时刷新 |
| 分身市场界面（灰度） | 前端 | 搜索/筛选/租赁流程可用 |
| 认知时间线导出功能 | 产品 | 导出延迟 < 10s |

**门禁条件**：端到端成功率 ≥ 95%，激活率 ≥ 85%

### Phase 2：审计与主网准备（Week 5–6）

**目标**：安全审计，提升LP深度，100人冷启动完成

| 任务 | 负责方向 | 完成标准 |
|------|---------|---------|
| Certora形式化验证（4个核心不变量） | 安全 | 不变量全部通过Prover |
| Trail of Bits / Slither静态审计 | 安全 | 无High/Critical未修复项 |
| AFC/USDC流动性池注入 | 经济 | LP深度 ≥ $50K |
| Speakable Schema标注集成（GEO P0） | 产品 | 引用计数链上同步成功 |
| 法律意见书出具（效用代币定性） | 合规 | Utility-Only分类确认 |
| 瑞士基金会/新加坡运营实体设立 | 合规 | 双司法辖区完成备案 |

**门禁条件**：审计报告无Critical，30日留存 ≥ 70%，NPS ≥ 60

### Phase 3：主网上线（Month 3–6）

**目标**：AFC主网1:1映射，跨链准备，生态扩展

| 里程碑 | 时间 | 验收指标 |
|--------|------|---------|
| AFC主网合约部署 | Month 3 | 与Base Sepolia状态一致性验证通过 |
| INP性能监控 + ECE校准集成 | Month 3 | 共振分采集实时，看板更新正常 |
| ZK实体验证器原型 | Month 4 | 验证延迟 < 800ms，Gas < 50K |
| 企业SDK发布（MCN接入） | Month 4 | ≥ 3家企业Pilot合同 |
| 跨链认知互操作（DECP协议）预研 | Month 5–6 | 技术可行性报告完成 |
| 认知发现协议（MCP扩展）上线 | Month 5–6 | 技能语义匹配 Top-N准确率 ≥ 80% |

---

## 六、CI/CD与部署规范

### 6.1 分支策略

- `main`：主网部署分支，仅通过审计合并
- `develop`：测试网部署分支，PR合并触发自动部署
- `feature/*`：功能分支，需通过CI（编译/测试/Slither）才能合并

### 6.2 自动化流水线

```yaml
# 每次PR触发：
- forge build --sizes
- forge test --gas-report
- forge coverage --report lcov（要求 ≥ 95%）
- Slither静态分析（High级别失败阻断合并）
- Gas报告自动评论至PR

# 打v*标签触发：
- 自动部署至Base主网
- Basescan源码验证
- 发送部署通知至Slack
```

### 6.3 环境配置

| 环境 | Chain ID | 用途 |
|------|---------|------|
| base-sepolia | 84532 | 功能测试与集成验证 |
| base-mainnet | 8453 | 生产环境，optimizer_runs=50000 |
| audit | — | 高优化+调试符号，供安全审计使用 |

---

## 七、告警规则（Prometheus）

| 告警名称 | 触发条件 | 严重级别 | 处理责任 |
|---------|---------|---------|---------|
| 激活率偏低 | 7日激活率 < 80% | Warning | 增长运营 |
| 首笔收益延迟 | 中位时间 > 16小时 | Critical | 产品工程 |
| 熔断触发异常 | 1小时触发率 > 12% | Warning | 风控工程 |
| 流动性池深度不足 | AFC/USDC池 < $50K | Critical | 经济工程 |
| 支付失败率偏高 | 1小时失败率 > 5% | Critical | 支付工程 |
| 共振分采集延迟 | 延迟 > 10s | Warning | 后端工程 |

---

## 八、合规接口预留规范

所有合规模块采用**插件式设计，默认关闭**，按需激活：

| 合规维度 | 接口名称 | 激活条件 |
|---------|---------|---------|
| 身份验证 | `KYCPlugin` | 企业客户或监管要求 |
| 收益申报 | `TaxLabelPlugin` | 特定司法辖区 |
| 数据隐私 | `ZKPrivacyPlugin` | GDPR/个人信息保护法 |
| 地理围栏 | `GeoCompliancePlugin` | 区域限制要求 |
| 争议解决 | `ArbitrationPlugin` | 高价值合约纠纷 |

合规模块通过流体民主投票决定是否激活特定区域策略。

---

*文档版本：v1.0 | 生成日期：2026年5月*
